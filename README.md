# RustScript website

Official website for RustScript, published at <https://rustscript.org/>.

## Development

```bash
bun install
bun run build
bun run preview
```

The production build writes `dist/`. The `public/CNAME` file keeps GitHub Pages on `rustscript.org`.
